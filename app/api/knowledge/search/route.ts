import { NextResponse } from "next/server";
import { createClient } from "@supabase/supabase-js";
export async function GET(request:Request){const url=new URL(request.url);const workspace=url.searchParams.get("workspace");const q=(url.searchParams.get("q")??"").trim();if(!workspace)return NextResponse.json({error:"Missing workspace"},{status:400});const db=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);let query=db.from("kb_articles").select("id,title,slug,excerpt,body_html,published_at,kb_categories(name)").eq("workspace_id",workspace).not("published_at","is",null).order("updated_at",{ascending:false}).limit(8);if(q)query=query.or(`title.ilike.%${q.replace(/[,%]/g,"")}%,excerpt.ilike.%${q.replace(/[,%]/g,"")}%`);const {data,error}=await query;if(error)return NextResponse.json({error:"Search unavailable"},{status:500});return NextResponse.json({articles:data??[]});}
